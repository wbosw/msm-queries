    <h1>Eldest director</h1>

<%= Director.where.not({:dob => nil}).all.order({:dob => :asc}).at(0).dob %>


<p>The eldest director in our dataset right now is <a href="/directors/<%= Director.where.not({:dob => nil}).all.order({:dob => :asc}).at(0).id %>"><%= Director.where.not({:dob => nil}).all.order({:dob => :asc}).at(0).name %></a>, born on May 18, 1897.</p>

<p>(<a href="http://strftime.net/" target="_blank">http://strftime.net/</a> is handy for putting together the formatting strings for the <code>.strftime</code> method.)</p>
